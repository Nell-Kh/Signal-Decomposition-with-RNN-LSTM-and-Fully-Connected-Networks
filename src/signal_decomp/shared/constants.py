"""Immutable project-wide constants — never hardcode these elsewhere."""

FREQUENCIES = [1, 3, 5, 7]   # Hz — fixed by assignment
NUM_FREQUENCIES = 4           # number of sinusoids
WINDOW_SIZE = 10              # samples per context window
VERSION = "1.00"              # project version